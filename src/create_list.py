from utils.list_utils import create_todo_list, add_item_to_list, remove_item_from_list, list_all_lists
import json

def main():
    # Config variable to update
    # LIST_NAME = "My New API List2"
    # create_todo_list(LIST_NAME)
    # add_item_to_list(LIST_NAME, "Buy groceries")
    # remove_item_from_list(LIST_NAME, "Buy groceries")
    # all_lists = list_all_lists()
    # print(json.dumps(all_lists, indent=2))

if __name__ == "__main__":
    main()
